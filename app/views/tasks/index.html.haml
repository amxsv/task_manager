%h1= t('.app_name')

%table
  %thead
    %tr
      %th= t('.todo')
      %th
      %th
      %th

  %tbody
    - @tasks.each do |task|
      %tr
        %td= task.title
        %td= link_to t('buttons.show'), task
        %td= link_to t('buttons.edit'), edit_task_path(task)
        %td= link_to t('buttons.destroy'), task, :method => :delete, :data => { :confirm => I18n.t('messages.are_you_sure') }

%br

= link_to t('buttons.new_task'), new_task_path
